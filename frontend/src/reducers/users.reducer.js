
const initialState = {
    currentUser: null,
    token: null,
    error: null,
    loading: false,

}

export default function users(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}